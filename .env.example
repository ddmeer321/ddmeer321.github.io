# Nur als Vorlage. Die echten Werte gehoeren in .env.local (wird ignoriert).
SNAP_CAMERA_KIT_API_TOKEN=DEIN_STAGING_API_TOKEN
SNAP_CAMERA_KIT_LENS_GROUP_ID=DEINE_DEMO_LENS_GROUP_ID

# Optional; Standardwerte sind 127.0.0.1 und Port 8915.
LOCAL_HOST=127.0.0.1
LOCAL_PORT=8915
